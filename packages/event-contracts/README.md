# Event contracts

Shared versioned event envelopes, runtime schemas, and TypeScript types for the SmartRetailX asynchronous order workflow.

All current event schemas accept only event version `1.0` and reject unknown fields.
